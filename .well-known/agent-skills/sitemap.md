# Sitemap discovery

This site publishes a sitemap at `/sitemap.xml`.

## Included URLs

The sitemap lists the canonical HTML entry points that should be indexed:

- The homepage
- The API documentation page

## Update model

The sitemap is generated during the publish build so it stays aligned with the configured canonical site URL and page list.

